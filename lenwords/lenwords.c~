#include "helpers.h"
#include <stdio.h>
#include <unistd.h>

int main() {
    while (1) {
        bytes = write_(STDOUT_FILENO, buf, bytes);
        if (bytes == -1)
            break;
    }
    return 0;
}
